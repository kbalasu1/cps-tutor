# Copy this file to .streamlit/secrets.toml for local development
# and fill in a real key. Never commit the real secrets.toml (it's
# already in .gitignore).
#
# On Hugging Face Spaces, set this instead via
# Settings -> Repository secrets -> GEMINI_API_KEY.

GEMINI_API_KEY = "your-gemini-api-key-here"
